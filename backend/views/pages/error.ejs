<%
const errorActions = [
    { label: 'На головну', href: '/', primary: true },
    { label: 'У кошик', href: '/cart' }
];
%>
<%- include('message', {
    messageIcon: 'error',
    statusCode: typeof statusCode !== 'undefined' ? statusCode : 500,
    messageTitle: typeof errorTitle !== 'undefined' ? errorTitle : 'Помилка',
    messageText: typeof errorMessage !== 'undefined' ? errorMessage : 'Сталася помилка',
    messageHint: '',
    messageActions: errorActions
}) %>
